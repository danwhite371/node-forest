type ChildrenProps = {
  children?: React.ReactNode;
};

type ClassNameProps = {
  className?: string;
};

type Location = 'home' | 'signin';

export type { ChildrenProps, ClassNameProps, Location };
